readonly ("false")

app_identifier("com.masters-academy.JSONMusic")

# username("ivizey")

team_id("ADH33X3GEE")

team_name("iOS Swift Advanced")

git_branch("homework") 

git_full_name("Pavel Bondar")

# git_user_email("ivizey@gmail.com")

shallow_clone ("true")

clone_branch_directly("false")

git_basic_authorization("Z2l0")

google_cloud_bucket_name("GCS")

google_cloud_project_id("Gkjnd15VVl-bj")

keychain_name("log.keychain")

keychain_password("IOSAdvanced")

force ("true")

force_for_new_devices("true")

skip_confirmation("true")

skip_docs("true")

platform("tvos")

template_name("Apple Pay")

output_path("Users/pavelbondar/Documents")

verbose("true")

git_url("https://github.com/Ivizey/Pass.git")

storage_mode("git")

type("enterprise") # The default type, can be: appstore, adhoc, enterprise or development

# app_identifier(["tools.fastlane.app", "tools.fastlane.app2"])
# username("user@fastlane.tools") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
